/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: ['127.0.0.1', 'localhost'],
  
  // Можно добавить другие настройки позже, если понадобится
  // reactStrictMode: true,
  // experimental: {
  //   turbopack: true,   // если хочешь явно включить
  // },
};

export default nextConfig;
